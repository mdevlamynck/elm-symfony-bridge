{
  "name": "elm-symfony-bridge",
  "private": "true",
  "workspaces": [
    "lib",
    "vite",
    "webpack"
  ],
  "scripts": {
    "test": "yarn --cwd lib test",
    "review": "yarn --cwd lib review",
    "check-format": "yarn --cwd lib check-format",
    "build-all": "yarn --cwd lib build && yarn --cwd vite build && yarn --cwd webpack build",
    "build-vite": "yarn --cwd lib build && yarn --cwd vite build",
    "build-webpack": "yarn --cwd lib build && yarn --cwd webpack build"
  }
}
